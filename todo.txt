# needed dataframes

- (extracted_project_data): funding start/end year
- project_institution_relations (project_id, institution_id) 25000
- project_person_relations (person_id,project_id) 151000
- people_joined_with_institutions (person_id,institution_id, i_address
- reduced_data_person (person_id, name, first_name, gender)


# Vorarbeits daten
## table v1 (johannes)
institution id, location

## table v2 (laura)
project_id, genderindex
	--> abgeleitet aus project_person_relation joined with reduced_data_person
		1. filter alle projecte <2 researchers
		2. groupieren nach project_id
		3. verhältnis ausrechnen (count male, count female)

# Enddaten
## table 1 (für kartenansicht)
institution_id, i_name, location, gender_index

	gender_index pro project berechnet. dann durchschnitt auf institutebene

## table 2 (für detailansicht)
instution_id, top_10_active_researchers, gender_index pro personm

[{institution_id:
	[{researcher_id, gender_index, name, anzahl_projekte}]}]

	top_10_active_researchers berechnet nach anzahl projecte

# TODO
- display gender_index (on hover?)
- display names instead of ids
- create scrollable in order to display a bit more information before showing the map
